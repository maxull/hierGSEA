% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hier_gsea.R
\name{hier_gsea}
\alias{hier_gsea}
\title{Process a GSEA result while respecting ontology hierarchy}
\usage{
hier_gsea(
  result,
  db,
  ontology = NULL,
  directional = "both",
  level_top = 1,
  level_bottom = NULL,
  alpha = 0.05,
  correction_method = "BH_family"
)
}
\arguments{
\item{result}{A \code{DOSE::gseaResult} object.}

\item{db}{Database name. Must be \code{"reactome"}, \code{"go"}, or \code{"mitocarta"}.}

\item{ontology}{Required when \code{db = "go"}. Must be one of \code{"BP"}, \code{"MF"},
or \code{"CC"}. Ignored for \code{"reactome"} and \code{"mitocarta"}.}

\item{directional}{Direction filter. \code{"both"} keeps all terms, \code{"up"} keeps
\code{NES > 0}, and \code{"down"} keeps \code{NES < 0}.}

\item{level_top}{Top hierarchy level to display.}

\item{level_bottom}{Bottom hierarchy level to display. Defaults to the
deepest level available after hierarchy mapping and directional filtering.}

\item{alpha}{Significance threshold used for branch retention and outline
labeling.}

\item{correction_method}{Currently only \code{"BH_family"} is supported.}
}
\value{
A \code{hier_gsea_result} object.
}
\description{
\code{hier_gsea()} takes a \code{gseaResult} object generated upstream by
\code{clusterProfiler} or \code{ReactomePA}, maps the tested terms onto a precomputed
hierarchy, performs family-wise Benjamini-Hochberg correction within sibling
families, retains the significant branches and their visible ancestors, and
returns a structured object ready for plotting or downstream inspection.
}
\details{
The function is intentionally opinionated about the order of operations:
hierarchy mapping happens first, then directional filtering, then level
windowing, then family-wise correction. This keeps the statistical families
aligned with exactly what the analyst is choosing to inspect.

To retain broad parent pathways such as top-level Reactome branches, users
should usually generate the upstream \code{gseaResult} with permissive size
filters, for example \code{minGSSize = 5}, \code{maxGSSize = 10000},
\code{pvalueCutoff = 1}, and \code{pAdjustMethod = "none"}. \code{hierGSEA} recomputes the
hierarchy-aware multiple-testing adjustment from the raw \code{pvalue} column, so
the upstream global adjusted p-values are not used directly.

For MitoCarta analyses, the upstream result should typically come from
\code{clusterProfiler::GSEA()} using \code{mitocarta_term2gene()} and
\code{mitocarta_term2name()} as the custom term mapping inputs.
}
