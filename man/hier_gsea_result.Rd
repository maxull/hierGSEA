% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hier_gsea_result-doc.R
\name{hier_gsea_result}
\alias{hier_gsea_result}
\title{Result object returned by \code{hier_gsea()}}
\description{
\code{hier_gsea()} returns a structured S3 object of class \code{hier_gsea_result}.
The object is designed to keep the original upstream \code{gseaResult}, the
hierarchy-aware result table, the full hierarchy metadata, and the plotting
coordinates together in one place so downstream analysis stays reproducible.
}
\section{Top-level elements}{

\describe{
\item{\code{result_raw}}{The original \code{DOSE::gseaResult} object supplied to
\code{\link[=hier_gsea]{hier_gsea()}}.}
\item{\code{results_tbl}}{The main hierarchy-aware output table after direction
filtering, visible-level trimming, family-wise BH correction, ancestor
retention, and branch ordering.}
\item{\code{hierarchy_tbl}}{The backend parent-child edge table for the selected
database. For GO this retains all available parent-child links, even when a
single canonical plotting parent is chosen later.}
\item{\code{paths_tbl}}{The backend term-path table used to recover root-to-node
lineage, canonical paths, and term levels.}
\item{\code{plot_tbl}}{A list containing plot-ready node and edge coordinates:
\code{nodes}, \code{edges}, and \code{verticals}.}
\item{\code{meta}}{A named list with analysis settings, recommended upstream
GSEA settings, backend version metadata, and the pre-pruning testing scope
table used for hierarchy-aware multiple-testing correction.}
}
}

\section{Key columns in \code{results_tbl}}{

\describe{
\item{\code{term_id}}{Database term identifier used to match the upstream GSEA
result onto the selected hierarchy backend.}
\item{\code{Description}}{Display label used for tables and plotting.}
\item{\code{NES}}{Normalized enrichment score from the upstream GSEA result.}
\item{\code{abs_NES}}{Absolute normalized enrichment score used by default for
point size.}
\item{\code{pvalue}}{Raw upstream GSEA p-value. This is the value used for the
hierarchy-aware BH recalculation.}
\item{\code{p.adjust}}{Incoming upstream global adjusted p-value, retained for
reference only.}
\item{\code{p_adjust_hier}}{Hierarchy-aware adjusted p-value recalculated within
visible hierarchy families.}
\item{\code{is_significant_hier}}{Logical flag indicating
\code{p_adjust_hier < alpha}.}
\item{\code{term_in_input}}{Logical flag marking whether the row came directly
from the upstream GSEA result (\code{TRUE}) or was added back as a retained
ancestor placeholder (\code{FALSE}).}
\item{\code{level}}{Visible hierarchy level used by \code{hierGSEA}, after applying
backend-specific ontology handling such as GO container-node removal.}
\item{\code{parent_id}}{Canonical parent used for family assignment and plotting.}
\item{\code{canonical_path}}{Canonical root-to-node path as a list column.}
\item{\code{family_id}}{Family label used for the sibling-wise BH correction at
the chosen visible level window.}
\item{\code{family_n_tested}}{Number of tested terms in that family before
branch pruning.}
\item{\code{branch_best_p}}{Best descendant \code{p_adjust_hier} within the retained
branch, used for ordering.}
\item{\code{branch_best_nes}}{Largest descendant absolute \code{NES} within the
retained branch, used as a tie-breaker for ordering.}
\item{\code{order_index}}{Final branch-preserving display order where parents
always appear before descendants.}
}
}

\section{Notes on interpretation}{

The \code{results_tbl} may contain non-significant ancestor rows with
\code{term_in_input = FALSE} and \code{NES = NA}. These rows are intentionally retained
so that significant descendants can be displayed inside an intact hierarchy
rather than as disconnected leaf terms.
}

