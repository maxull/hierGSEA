% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hier_gsea.R
\name{plot_hier_gsea}
\alias{plot_hier_gsea}
\title{Plot a hierarchy-aware GSEA result}
\usage{
plot_hier_gsea(
  x,
  label_col = "Description",
  size_col = "abs_NES",
  colour_col = "p_adjust_hier",
  show_left_hierarchy = TRUE,
  tree_width = 0.32,
  top_n_parents = NULL,
  parent_terms = NULL,
  significance_cutoff = 0.05
)
}
\arguments{
\item{x}{A \code{hier_gsea_result} object returned by \code{\link[=hier_gsea]{hier_gsea()}}, or a
non-empty list of compatible \code{hier_gsea_result} objects.}

\item{label_col}{Label column to display beside the hierarchy.}

\item{size_col}{Column used for point size.}

\item{colour_col}{Column used for point fill color.}

\item{show_left_hierarchy}{Logical. If \code{TRUE}, draw the hierarchy connector
panel on the left.}

\item{tree_width}{Width of the left-side hierarchy strip in plot x-units.
Increase this when the parent-child structure needs more horizontal room.}

\item{top_n_parents}{Optional number of top-ranked starting-level branches to
show. This is applied after the hierarchy-aware ordering has already been
computed, so \code{1} means "show the highest-ranked visible branch at
\code{level_top}".}

\item{parent_terms}{Optional character vector, or list coercible to a
character vector, containing starting-level term labels or IDs to show.
This is useful when you want to focus the plot on specific major branches
from the chosen \code{level_top}. If supplied, \code{top_n_parents} is ignored.}

\item{significance_cutoff}{Adjusted p-value threshold used for the white
midpoint in the fill scale and for the optional outline marker. Defaults
to \code{0.05}.}
}
\value{
A \code{ggplot2} object.
}
\description{
The plot keeps the hierarchy visible directly in the graphic. The left side
is a compact parent-child tree strip, while the right side shows a dot plot
for enrichment strength and hierarchy-aware adjusted p-values. When the
result was processed with \code{directional = "both"}, the plot separates negative
and positive enrichment into \code{Down} and \code{Up} columns so the sign is retained
visually without sacrificing the shared hierarchy layout. The same function
also accepts a list of compatible \code{hier_gsea_result} objects and will then
draw one shared hierarchy with the comparison blocks arranged from left to
right in the order of the list.
}
